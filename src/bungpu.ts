throw new Error('webgpu bindings are not implemented!');
//export class BunGPU implements GPU {
//readonly __brand: 'GPU' = 'GPU';

//constructor(
//public readonly textureFormat: GPUTextureFormat = 'bgra8unorm',
//public readonly adapter: GPUAdapter = new BunGPUHeadlessAdapter()
//) { }

//async requestAdapter(
//options?: GPURequestAdapterOptions
//): Promise<GPUAdapter | null> {
//return this.adapter;
//}

//getPreferredCanvasFormat(): GPUTextureFormat {
//return this.textureFormat;
//}
//}

//export class BunGPUHeadlessAdapter implements GPUAdapter {
//readonly __brand: 'GPUAdapter' = 'GPUAdapter';

//readonly imits

//readonly features: GPUSupportedFeatures = new Set<GPUFeatureName>([]);
//}
